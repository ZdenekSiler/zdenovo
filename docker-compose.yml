services:
  web:
    build: .
    ports:
      - "8080:8000"
    volumes:
      - db_data:/data
    environment:
      - DB_DIR=/data
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    restart: unless-stopped

volumes:
  db_data:
